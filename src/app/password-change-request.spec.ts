import { PasswordChangeRequest } from './password-change-request';

describe('PasswordChangeRequest', () => {
  it('should create an instance', () => {
    expect(new PasswordChangeRequest()).toBeTruthy();
  });
});
